node_modules/
dist/
out/
release/
release-zip/
# Leading ** matters: a pattern with a slash in the middle is anchored to this
# file's directory, so a bare `vendor/ffmpeg/` would stop matching the moment
# the app moved under apps/.
**/vendor/ffmpeg/
*.local
.DS_Store
*.tsbuildinfo

.ffmpeg-build/

/.agentdocs/
/AGENTS.md
/DESIGN.md
/spec

.playwright-mcp
/.claude/skills/skill-publish
